package storeapp.domain.enums;

// Este enum define los diferentes roles que un administrador puede tener en el sistema. Cada rol puede tener diferentes permisos y responsabilidades asociados, lo que permite una gestión más flexible y segura de las funciones administrativas dentro de la aplicación.
public enum AdminRole {
    SUPERADMIN,
    MODERADOR,
    OPERADOR
}

